# 2026-09-04 学习计划：完成 Day 1 未完成内容

## 明日目标

明天不急着追赶原定进度。核心目标是：

> 完成并通过检查点 B，再完成检查点 C 的四个基础练习。

如果检查点 B 和 C 均扎实完成，再开始 Vector Add；不以赶进度为目标。

## 09:00-09:20：闭卷回忆

- 不看笔记，写出最小 Hello World。
- 解释 `#include`、`main`、`std::cout` 和 `return 0`。
- 写出一个字符串变量和一个整数变量，并初始化。
- 写出读取姓名并输出问候语的代码。

完成后再对照 `day1/checkpoint_B_cpp_basics_partial.md`，用另一种颜色或单独小节修正错误。

## 09:20-10:30：补齐检查点 B

重点学习：

- `int`、`float`、`double` 的区别。
- 初始化与赋值。
- `=` 与 `==`。
- `if` 条件语句。
- `for` 和 `while` 循环。
- 下标从 0 开始的基本含义。

学习方式：每学一个概念，先写一个 5-15 行的小程序验证，不只阅读文字。

## 10:30-11:10：检查点 B 口述验收

脱离教材回答七个基础问题，并接受变式追问。不会的问题先请求 L1 或 L2 提示，不直接索要完整答案。

通过标准：

- 能用自己的话解释，不依赖背诵。
- 能为每个概念写一个简单例子。
- 能分清编译错误、运行结果与变量值。

## 11:10-12:00：微型练习准备

独立完成：

1. 输入两个整数，输出和、差、积。
2. 安全地处理除法，避免除以零。

结束前记录至少一个编译错误或逻辑错误及其修复过程。

## 12:00-13:30：午休

离开屏幕，充分休息。

## 13:30-15:00：完成检查点 C

创建 `day1/basics.cpp`，完成：

1. 输入两个整数，输出和、差、积和商。
2. 输入 `N`，输出 `0` 到 `N-1`。
3. 计算 `1 + 2 + ... + N`。
4. 输入五个浮点数，输出最大值和平均值。

要求：

- 考虑 `N=0`。
- 不发生除以零。
- 使用 `-Wall -Wextra` 编译。
- 每做完一题先解释思路，再运行验证。

## 15:00-15:20：休息

## 15:20-16:10：检查点 C 验收与修改

- 检查正常输入。
- 检查零、负数等边界输入。
- 解释变量类型的选择。
- 解释每个条件和循环为何正确。
- 根据代码审查修改，但不复制完整答案。

## 16:10-17:00：整理与 Git 提交

更新检查点 B 的状态，整理错误记录，然后分开提交：

```text
docs: complete checkpoint B C++ basics notes
feat: complete checkpoint C basic exercises
```

提交前检查：

```powershell
git status
git diff
```

提交后推送：

```powershell
git push
```

## 可选加餐：Vector Add

仅当 B、C 都通过并且精神状态良好时开始：

- 写清输入、输出和计算规则。
- 先写伪代码，不直接寻找完整实现。
- 创建最小测试数据验证。

如果当天没有进入 Vector Add，不算落后。基础练习能独立完成比赶进度更重要。

## 明日结束标准

- [ ] 检查点 B 通过口述和变式验收。
- [ ] `basics.cpp` 四项练习能够独立编译运行。
- [ ] 能解释 `=`、`==`、条件、循环和数据类型。
- [ ] 笔记包含错误、尝试和最终理解。
- [ ] 本地提交已经推送到当前 Day 1 分支。
